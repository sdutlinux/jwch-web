<%= form_for [:admin,@education_project], :html => { :class => 'form-horizontal' } do |f| %>
  <fieldset>
    <legend><%= controller.action_name.capitalize %> /Education Project</legend>

    <div class="control-group">
      <%= f.label :name, :class => 'control-label' %>
      <div class="controls">
        <%= f.text_field :name, :class => 'text_field' %>
      </div>
    </div>

    <div class="control-group">
      <%= f.label :year, :class => 'control-label' %>
      <div class="controls">
        <%= f.date_select :year, :class => 'date_select' %>
      </div>
    </div>

    <div class="control-group">
      <%= f.label :department, :class => 'control-label' %>
      <div class="controls">
        <%= f.text_field :department, :class => 'text_field' %>
      </div>
    </div>

    <div class="control-group">
      <%= f.label :team, :class => 'control-label' %>
      <div class="controls">
        <%= f.text_field :team, :class => 'text_field' %>
      </div>
    </div>

    <div class="control-group">
      <%= f.label :rank, :class => 'control-label' %>
      <div class="controls">
        <%= f.text_field :rank, :class => 'text_field' %>
      </div>
    </div>

    <div class="form-actions">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to 'Cancel', admin_education_projects_path, :class => 'btn' %>
    </div>
  </fieldset>
<% end %>
