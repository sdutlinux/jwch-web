<div class="page-header">
  <span class="section-title"> 所有学院 </span>
  <%= link_to '添加新分类', new_admin_section_category_path(@section), :remote => true, :class => 'btn pull-right' %>
</div>

<table class="table table-striped">
  <thead>
    <tr>
      <th>序号</th>
      <th>标题</th>
      <th>发布于</th>
      <th>动作</th>
    </tr>
  </thead>
  <tbody>
    <% @course_types.each do |course_type| %>
      <tr id="category_<%= course_type.id %>">
        <td><%= course_type.id %></td>
        <td><%= link_to course_type.name, admin_category_courses_path(course_type) %></td>
        <td><%= course_type.created_at %></td>
        <td>
          <%= link_to '编辑', edit_admin_section_category_path(:section_id => course_type.section_id, :id => course_type.id ), :remote => true, :class => 'btn btn-mini' %>
          <%= link_to '删除', admin_section_category_path(:section_id => course_type.section_id, :id => course_type.id), :method => :delete, :remote => true, :confirm => 'Are you sure?', :class => 'btn btn-mini btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
<p><%= will_paginate @course_types, :renderer => BootstrapPagination::Rails %></p>
