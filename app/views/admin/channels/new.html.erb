<h3>添加栏目</h3>
<fieldset>
  <div class="control-group">
    <%= form_for [:admin,@channel] do |f|%>
      栏目名称:<%= f.text_field :channelname %><br>
      栏目排序:<%= f.text_field :sort, :size => 10%><br>
      <%= f.submit "创建", :class => 'btn btn-primary' %>
  <% end %>
</div>
</fieldset>
