<%= form_for [:admin,@link], :html => { :class => 'form-horizontal' } do |f| %>
  <fieldset>
    <legend><%= controller.action_name.capitalize %> 链接</legend>

    <div class="control-group">
      <%= f.label "链接", :class => 'control-label' %>
      <div class="controls">
        <%= f.text_field :name, :class => 'text_field' %>
      </div>
    </div>

    <div class="control-group">
      <%= f.label "排序", :class => 'control-label' %>
      <div class="controls">
        <%= f.number_field :sort, :class => 'number_field' %>
      </div>
    </div>

    <div class="control-group">
      <%= f.label "链接", :class => 'control-label' %>
      <div class="controls">
        <%= f.text_field :url, :class => 'text_field' %>
      </div>
    </div>

    <div class="form-actions">
      <%= f.submit '提交', :class => 'btn btn-primary' %>
      <%= link_to '取消', admin_links_path, :class => 'btn' %>
    </div>
  </fieldset>
<% end %>
